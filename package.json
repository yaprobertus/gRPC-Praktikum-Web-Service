{
  "name": "praktikum-web-service-week-4",
  "version": "2.0.0",
  "private": true,
  "description": "GraphQL API Sesi 04-05: Next.js, Apollo Server, Neon, nested resolver, mutation, dan audit N+1.",
  "type": "module",
  "engines": { "node": ">=20.9" },
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "db:migrate": "node src/db/migrate.js",
    "db:seed": "node src/db/seed.js",
    "db:setup": "npm run db:migrate && npm run db:seed",
    "test": "node --test",
    "check": "node --check api/graphql.js && node --check src/schema/resolvers.js && node --test && next build"
  },
  "dependencies": {
    "@apollo/server": "^5.0.0",
    "@as-integrations/next": "^4.0.0",
    "dataloader": "^2.2.3",
    "dotenv": "^17.2.2",
    "graphql": "^16.11.0",
    "next": "^16.3.5",
    "pg": "^8.16.3",
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  }
}
